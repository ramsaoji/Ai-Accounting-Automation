# API Base URL configuration for the Vite React frontend
# Make sure to point this to the backend server (e.g. localhost:8080 or deployed production URL)
VITE_API_BASE_URL=http://localhost:8080

# Business display name shown throughout the frontend UI (default: Hotel Gaurav)
VITE_BUSINESS_NAME=Hotel Gaurav
